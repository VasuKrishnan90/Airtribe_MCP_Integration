# Environment Configuration Template
RESUMES_DIR=data/resumes
JOBS_DIR=data/jobs
OUTPUT_DIR=data/output
SANDBOX_ROOT=.

# Optional LLM API Key (if connecting agent to live Gemini API; agent includes mock fallback)
GEMINI_API_KEY=
